# 1. Ignore everything with a dot (your global rule)
*.*

# 2. Allow the folders themselves (so Git can enter them)
!FW8051Data/
!CodeTemplates/
!Solutions/

# 3. Allow EVERY file inside those specific folders 
# This overrides the *.* rule for these specific paths
!FW8051Data/**
!CodeTemplates/**
!Solutions/**

# 4. Explicitly allow the root gitignore and README
!.gitignore
!README

# 5. Re-ignore the things you definitely don't want
.Rproj.user
/.quarto/